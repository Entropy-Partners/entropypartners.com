# entropypartners.com
One static page. index.html is the whole site.
Preview: run `python3 -m http.server 8000` in this folder and open http://localhost:8000/.
The raster images (favicon.ico, apple-touch-icon.png, icon-512.png, og.png) are regenerated by tools/make_images.py; favicon.svg is hand-written.
